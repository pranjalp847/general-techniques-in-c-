#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main()
{
      for(int i=0;i<m;i++)
    {
        vector<int> v;
        v.clear();
        int a=0;
    	while(n!=0)
    	{
        	a=n%10;
        	n=n/10;
        	v.push_back(a);
    	}
    	//cout << "Vector size = " << v.size() << endl;
    	int l=v.size()-1;
        string s="";
    	for(int i=l;i!=-1;i--)
        {
            //sum=sum+(v[i]+k)*pow(10,i-1);
            s=s+to_string(v[i]+k);
        }
        n=stoi(s);
        
        //cout << n << endl;
    }
    
   // cout << "ANswer" << endl;
    
    string s = to_string(n);
    
    return s.length();

    return 0;
}
